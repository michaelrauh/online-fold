#lang racket
(provide get-origin make-ortho ortho-size ortho-origin ortho-hops)

(define (get-origin o)
  1)

(define (make-ortho a b c d)
  1)

(define (ortho-size o)
  1)

(define (ortho-origin o)
  1)

(define (ortho-hops o)
  (list 1 1))

; size is a multiset of numbers. Length of each axis. This is the occurrence set of the path to the bottom right corner.